export enum OP_STATUS {
  IDLE = 'Click the above button to see all the artists.',
  SUCCESS = 'Success',
  FAILED = 'Error Occured please try again.',
  LOADING = 'Loading...',
  DELETING = 'Deleting...'
}

export enum CRUD_TYPE {
  ADD,
  UPDATE,
  DELETE,
  DISPLAY,
}
