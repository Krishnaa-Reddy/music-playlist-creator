import { Constants } from "src/app/constants/common";
import { Environment } from "src/app/interfaces/environment";

export const environment : Environment = {
    title: Constants.title + '(dev)',
};
